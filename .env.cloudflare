# Configuración para Cloudflare Worker
# Copia este archivo a .env.local después de configurar tu Worker

# URL de tu Cloudflare Worker
# Reemplaza 'your-account' con tu ID de cuenta de Cloudflare
REACT_APP_WORKER_URL=https://wpt-config-api.your-account.workers.dev

# Opcional: Base URL de tu aplicación en producción
REACT_APP_BASE_URL=https://your-production-domain.com

# Modo de configuración (development/production)
REACT_APP_CONFIG_MODE=cloudflare-worker
